<div class="row">
  <div class="col-lg-12">
    <div class="panel">
      <div class="panel-heading">
        <%= @klass.name %>
        <%= link_to edit_class_path(@klass), class: 'pull-right' do %>
          <i class="fa fa-edit"></i>
        <% end %>
      </div>
      <div class="panel-body">
        <dl class="dl-horizontal">
          <dt>Día de la Semana:</dt>
          <dd>
            <%= @klass.day_of_week %>
          </dd>
          <dt>horas:</dt>
          <dd>
            <%= l @klass.class_time, format: :time %>
          </dd>
          <dt>Recurrente:</dt>
          <dd>
            <%= @klass.recurring %>
          </dd>
        </dl>
      </div>
    </div>
  </div>
</div>
